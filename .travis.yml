sudo: false

language: cpp

cache:
  apt: true

addons:
  apt:
    sources:
    - boost-latest
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - g++-5
    - libboost1.55-all-dev

compiler:
    - g++-5

script:
    - mkdir _builds
    - cd _builds
    - cmake ..
    - make
    - ctest
